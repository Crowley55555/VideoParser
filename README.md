# VideoParser

Простой и эффективный инструмент для массовой загрузки видео и других файлов по ссылкам.

## Возможности

- 📥 Массовая загрузка файлов из списка URL-адресов
- 🎬 Поддержка YouTube (скачивание видео через `yt-dlp`)
- ⚡ Параллельная загрузка с использованием многопоточности (до 4 одновременно)
- 📊 Отображение прогресса загрузки в реальном времени
- 📁 Автоматическое создание директории назначения
- 💾 Сохранение оригинальных имен файлов или названий видео
- 🛡️ Обработка ошибок и продолжение работы при сбоях

## Установка

1. Убедитесь, что у вас установлен Python 3.6+
2. Установите необходимые зависимости:

```bash
pip install requests yt-dlp
```

## Использование

1. Отредактируйте `main.py` и добавьте нужные ссылки в список `links`:

```python
links = [
    "https://youtu.be/VIDEO_ID",
    "https://www.youtube.com/watch?v=VIDEO_ID",
    "https://example.com/direct_file.mp4",
]
```

2. Запустите скрипт:

```bash
python main.py
```

3. Укажите нужную директорию для сохранения файлов, изменив параметр `save_dir`:

```python
download_files(links, save_dir=r"D:\data")
```

YouTube-ссылки автоматически определяются и скачиваются через `yt-dlp` в формате MP4.
Остальные URL загружаются напрямую через HTTP.

### Если YouTube пишет «Sign in to confirm you're not a bot»

Используй **файл с куками** (работает с любым браузером, в т.ч. Comet):

1. Установи расширение для экспорта куки:
   - **Chrome / Comet / Edge:** [Get cookies.txt LOCALLY](https://chromewebstore.google.com/detail/get-cookiestxt-locally/cclelndahbckbenkjhflpdbgdldlbecc)
   - **Firefox:** [cookies.txt](https://addons.mozilla.org/firefox/addon/cookies-txt/)
2. Открой [youtube.com](https://youtube.com) и убедись, что ты залогинен.
3. Экспортируй куки в формате **Netscape** и сохрани файл как `cookies.txt` в папку с проектом (рядом с `main.py`).
4. Запусти `python main.py` — парсер сам подхватит `cookies.txt`.

Файл `cookies.txt` добавлен в `.gitignore`, в репозиторий он не попадёт.

## Пример вывода

```
📥 Загрузка YouTube: https://youtu.be/WcjQQLBHZTE
  → Название видео.mp4                                  75.3% (2.5MiB/s)
✅ Скачано: Название видео.mp4
✨ Все загрузки завершены.
```

## Лицензия

Этот проект распространяется под лицензией MIT. Подробнее см. в файле [LICENSE](LICENSE).
